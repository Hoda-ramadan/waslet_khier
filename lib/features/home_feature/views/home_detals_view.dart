import 'package:flutter/material.dart';

class HomeDetalsView extends StatelessWidget {
  const HomeDetalsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}